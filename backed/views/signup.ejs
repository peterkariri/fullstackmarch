how do you handle dynamic html {
    thos refers to html that has javascript and its passed with ejs syntax {
   this values are output in the html {you will have styled the dashbiard with css and html }
        output {
            <%= value %>
             <%-value %>


        }
   script tag >we use this tags to run javascriptcode  such as conditional statemets,loops ,
   it runs the code but does not print to the output {

    <%  code here %>

    loopps through the script {
     %> 
      <%
      <ul>
        <% 
        items.forEach(item =>{   %> 

       <%  })
        %>
      </ul>
    }

   
   }
    }
}