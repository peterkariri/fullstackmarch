create a folder called gitNew
create three files in it 
open it via vs code using the terminal

github vs git 

github>>>an opensource platform where developers post their code publicly or privately 
it can be used for a wide variery of things(hosting ,interaction)

git>>version control tool that checks and tracks the versions of code in the codebase on your local 
machine and compares it to the one pushed to github so that we keep track of changes 

with the help of git commands we get to see these manipulations in action 
//git commands{
this commands are used by git 
set up commands 
we use this to check the username and the email 

git config --global user.name "your name"
git config --global user.email "your email"
after setting this up we run git config --list to check the set up status 



}

to start a git repository =(a storage reference for your project)
git init>>to initialize the repo

check status of git