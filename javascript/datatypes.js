/* a datatype is a classification or type of how we want to represn data ,
we specify the type of data tha a variable can hold {
    we have two categories of data types {
        primitive data types 
        non-primitive data types 
    }
}

primitive data types>>these are data types the are non object and do not have methods (we cannot change them ){
    strings
    booleans
    number 
    NaN
    undefined 

to define any data type we use variables 

} */
/* strings >>this are a set of characters that can be combined to form and represent somethings (names,sentences ) */
let firstName="Purity"

let firstSentence="I am leanring javascript "

console.log(firstSentence);


//number>>number is integers ,decimals and any fractional summation ,any mathematical value 
//we dont use quotes 
let id=12344545;

//boolean>>is the logical operation done on something and it executes whether true or false 

let itsRaining=false;

if(itsRaining){
    console.log("today is rainng ,we wont go to the tech and ai  academy");
    
}
else{
    console.log("we are going to do js in tech and ai academy ");
    
}

//undefined>>means that a variable is been called but its value was not create,named or passed 

let schoolName;

console.log(schoolName);

//null>>a variable has been created but the value has been left empty by specifying the null value which means the varible is to be later 
//assigned a value 

let y=null;

console.log(y);

y=5+5

console.log(y);


