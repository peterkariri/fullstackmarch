/* the Dom STANDS FOR DOCUMENT OBJJECT MODEL (HOW THE HTML CONTENT IS TRANSFOMRED INTO NODES){
    EVERY HTML TAGS IS TRANSFORMED NTO NODES JUST LIKE A TREE WITH BRANHE AND LEAVES WO TJHAT YOU CAN ACCESS EVERY BRANCH AND LEAF USING JAVASCRIPT 
    THIS OCCURS AFTER APAGE HAS BEEN  INTERPRETED AND NOW IT IS BEEN  RENDERED  TO THE USER 
    TO DO THIS JS USES THE dom TO MANIOULATE ,CHAnge and alter the content 
    to create responsiveness or content change 
}
to do this we use selctors that enable us to target the elements that we want to manipulate{
 to define this ods we chain them with the document as this methods belong to the document object 
    there are various selectors used here {
        id{
           document.getElementById("id")
        }
        tag 
        {
            document.getElementsByTagName('tagname')
        }
        class {
            document.getElementByclassName("clasname")
        }
   al these should be declared in and assigned to variables and they should be declred in the beginnning of the js file. 
    }
} */